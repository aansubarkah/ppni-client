import { tableViewerItem } from '../../../helpers/table-viewer-item';
import { module, test } from 'qunit';

module('Unit | Helper | table viewer item');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tableViewerItem(42);
  assert.ok(result);
});
