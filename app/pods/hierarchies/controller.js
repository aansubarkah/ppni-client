import Ember from 'ember';

export default Ember.Controller.extend({
    title: 'Struktur Organisasi PPNI Jatim',
    columnsName: [
        {
            "name": "name",
            "title": "Nama"
        }
    ]
});
